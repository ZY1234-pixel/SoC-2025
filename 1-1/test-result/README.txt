本目录仅保留说明文件，真实测试输出不提交到 GitHub。

如果需要共享测试结果，请统一上传到百度网盘，并在顶层 `README.md` 中补充链接。

每次执行 `Code/test.py` 后，程序会在本目录下创建一个新的：
- `run_YYYYMMDD_HHMMSS/`

结果会按样例分层放在：
- `run_YYYYMMDD_HHMMSS/samples/<样例名>/`

每个样例目录下通常包含：
- `<样例名>.json`
- `<样例名>.docx`
- `<样例名>.md`
- `<样例名>.pdf`
- `<样例名>_assets/`
- `debug/`
