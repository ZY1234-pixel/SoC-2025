"""纸张尺寸、单位换算与版面常量。"""

from typing import List, Tuple

# ---------------------------------------------------------------------------
# 标准纸张尺寸: (名称, 宽度_mm, 高度_mm) -- 纵向
# ---------------------------------------------------------------------------
PAGE_SIZES: List[Tuple[str, float, float]] = [
    # 常见尺寸优先 —— 宽高比相同时优先匹配（如 A4 vs A3）
    ("A4", 210.0, 297.0),
    ("Letter", 215.9, 279.4),
    ("A5", 148.0, 210.0),
    ("B5", 176.0, 250.0),
    ("A3", 297.0, 420.0),
    ("B4", 250.0, 353.0),
    ("Legal", 215.9, 355.6),
    ("Tabloid", 279.4, 431.8),
]

# ---------------------------------------------------------------------------
# A4 尺寸（磅，1 pt = 1/72 英寸）
# ---------------------------------------------------------------------------
A4_WIDTH_PT: float = 595.28
A4_HEIGHT_PT: float = 841.89

# ---------------------------------------------------------------------------
# 默认页边距（磅）
# ---------------------------------------------------------------------------
DEFAULT_MARGIN_PT: float = 54.0

# ---------------------------------------------------------------------------
# 最小行距（磅）
# ---------------------------------------------------------------------------
MIN_LINE_SPACING_PT: float = 0.7

# ---------------------------------------------------------------------------
# 单位转换因子
# ---------------------------------------------------------------------------
TWIPS_PER_PT: int = 20
EMU_PER_PT: int = 12700
MM_PER_INCH: float = 25.4
PT_PER_INCH: float = 72.0

# ---------------------------------------------------------------------------
# 区块类型标签（版面分析支持的所有类别 —— v2.0）
# ---------------------------------------------------------------------------
BLOCK_TYPE_LABELS: List[str] = [
    "text",
    "title",
    "table",
    "figure",
    "figure_caption",
    "table_caption",
    "table_footnote",
    "formula",
    "formula_caption",
    "header",
    "footer",
    "page_number",
    "reference",
    "abstract",
    "code",
    "list",
    "footnote",
    "watermark",
    "abandon",
]

# ---------------------------------------------------------------------------
# 以图像/非文本流方式渲染的类型
# ---------------------------------------------------------------------------
# 注意：必须使用 BlockType 枚举成员而非字符串，
#       因为版面代码通过 ``blk.block_type in FIGURE_TYPES`` 比较。
from docflow.model.base import BlockType as _BlockType

FIGURE_TYPES = frozenset({
    _BlockType.FIGURE,
    _BlockType.FORMULA,
})

# 可跨列分配的区块类型（不作为文本块聚类）
SPAN_ELIGIBLE_TYPES = frozenset({
    _BlockType.FIGURE,
    _BlockType.FORMULA,
    _BlockType.FIGURE_CAPTION,
    _BlockType.TABLE_CAPTION,
    _BlockType.FORMULA_CAPTION,
})

# ---------------------------------------------------------------------------
# 版面分析阈值
# ---------------------------------------------------------------------------
MAX_COLS: int = 4
COLUMN_CLUSTER_THRESH: float = 0.13
WIDE_BLOCK_RATIO: float = 0.55
Y_OVERLAP_MIN_PX: int = 4

# ---------------------------------------------------------------------------
# 段落检测
# ---------------------------------------------------------------------------
PARAGRAPH_INDENT_MIN_PX: float = 12.0
PARAGRAPH_GAP_RATIO: float = 1.4
